# Base URL for the backend API.
# Leave empty to use relative URLs (works with the Vite dev proxy and Nginx in production).
VITE_API_BASE=

# Backend URL used by the Vite dev proxy (Node-side, not shipped to browser).
# Default matches the dev backend on port 8001.
VITE_API_URL=http://backend:8001
